#include <stdio.h>
#include <stdlib.h>

void lerVet(int vet[]){
    for(int i = 0; i < sizeof(vet/sizeof(int);i++){
        printf("Digite o %d numero: ",i);
        scanf("%d",vet[i]);
    }
}
        
int main(){
    int vet[10]
    letVet(vet);
    return 0;
}
